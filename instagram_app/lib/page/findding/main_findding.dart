import 'package:flutter/material.dart';

class MyMainSearch extends StatelessWidget {
  const MyMainSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
